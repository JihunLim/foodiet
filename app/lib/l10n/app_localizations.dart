/// Placeholder delegate — MVP 부팅 시에는 `flutter gen-l10n` 으로
/// 자동 생성된 파일이 이 자리를 대체한다. 이 파일은 초기 빌드/리뷰용 stub.
///
/// 실제 사용 시 `pubspec.yaml` 의 `generate: true` 에 의해
/// `lib/l10n/app_ko.arb` / `app_en.arb` 로부터 `AppLocalizations` 클래스가 생성됨.
library;

// 생성 후에는 이 파일을 삭제하고 `package:flutter_gen/gen_l10n/app_localizations.dart` 를 import.
