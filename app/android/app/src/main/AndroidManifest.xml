<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ─── 권한 ──────────────────────────────────────────────────────────
         iOS Info.plist 의 NSCameraUsageDescription / NSPhotoLibraryUsage… /
         UNUserNotificationCenter 등에 대응되는 Android 권한 선언.
         13(=API 33) 이상의 알림·미디어 분리, 14 의 exact alarm 정책까지 포함. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- 사진 라이브러리(메뉴 사진 첨부용). Android 13+ 는 granular media. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- ~Android 12 : 레거시 외부 저장. 13+ 에서는 자동 무시. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <!-- FCM + flutter_local_notifications: Android 13+ 부터 런타임 요청 필요. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- 식사 reminder 로컬 알림 정밀 예약. SCHEDULE 은 12+, USE 는 14+ Play 정책. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <!-- 카메라 기능을 직접 사용. tablet 등 카메라 없는 디바이스에서도 보여야 하면
         required=false 로 바꿀 수 있음. 일단 식단 기록 핵심 기능이라 required. -->
    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <application
        android:label="foodiet"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- 홈스크린 위젯 탭 → foodiet://widget/<target> 딥링크 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="foodiet" android:host="widget"/>
            </intent-filter>
        </activity>

        <!-- foodiet 홈스크린 위젯 (§4.1 / §4.4 / §4.5) -->
        <receiver
            android:name=".widget.QuickLogWidgetProvider"
            android:exported="true"
            android:label="foodiet · 한 장 찍기">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/fd_quick_log_info"/>
        </receiver>
        <receiver
            android:name=".widget.RemainingWidgetProvider"
            android:exported="true"
            android:label="foodiet · 남은 칼로리">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/fd_remaining_info"/>
        </receiver>
        <receiver
            android:name=".widget.CoachTipWidgetProvider"
            android:exported="true"
            android:label="foodiet · 푸디의 한마디">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/fd_coach_info"/>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- AdMob 앱 ID. Google Mobile Ads SDK 가 부팅 시 읽는다. 누락 시 크래시. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-6523753930426193~9177349187" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
